# 更新日志

## [2.9.4]

### 新增

- **TTS语音控制增强**
  - 新增语速控制滑块（0.5-2.0），支持实时调节语速
  - 新增音调控制滑块（-50Hz ~ +50Hz），支持降低或提高音调

---

## [2.9.3] - 2026-08-28

### 修复

- **表情包识别逻辑**
  - 修复无文字图片转表情描述时，识别结果中的"无 / 无文字 / 没有文字 / 图中无文字"等表达被误当成表情描述传给对话模型的问题，新增 `_is_no_text_value` 过滤 20 余种"无文字"变体。
  - 修复"卡通可爱风格表情包"描述前缀被改写，导致后续 `startswith("表情包")` 判断失效的问题，改为保留"表情包"前缀。
  - 加强表情包/卡通风格的文件名与 URL 特征识别，新增 `EMOJI_FILE_KEYWORDS`、`CARTOON_STYLE_KEYWORDS` 关键词库（meme、biaoqing、表情、梗图、贴纸等）。
  - 重写识图结果解析，使用正则 `_VISION_LABEL_PAT` 解析"类型/文字/描述"字段，兼容多行与同行混合输出格式。
  - 加强图片类型归类 `_classify_vision_type`，扩展表情包/普通图片的识别关键词。
  - 表情包转对话描述统一控制在 30 字以内。

### 新增

- **控制界面 API Key 掩码显示**
  - API Key 保存后以圆点掩码显示，不再展示明文；未配置时输入框保持为空、无提示文字。
  - 后端配置接口（读取与保存）对敏感字段一律脱敏，并新增 `api_key_set` 标识，避免真实 Key 回显。
  - 保存掩码占位（含 `*` 或 `•`）视为未改动，保留已保存的 Key；输入新 Key 保存即可替换，清空保存表示移除。
