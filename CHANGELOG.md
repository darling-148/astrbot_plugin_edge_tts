# CHANGELOG

## 3.0.3

### 改动

- **移除消息 Emoji 表情识别**：删除 `enable_emoji_analysis` 配置项及相关功能，不再识别用户消息中的 Emoji 表情含义并注入提示。采用模型识别，不然其他国家语音发送表情有注入文字感染。
- **回复无字符上限**：移除 `tts_max_length` 配置项及文字/语音截断逻辑，回复内容不再有长度限制。
- **TTS 保留括号内容**：语音播报时不再过滤表情/情感/动作括号（如 `（微微一笑）`、`（摊手）`），保留该部分，语气词跟人格有关，和TTS无关想屏蔽该词建议改人格。   原样读出；Emoji 符号原样传入 TTS 不翻译。

###回复加速
