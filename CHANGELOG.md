# Changelog

所有值得关注的变更都将记录在此文件中。

## [2.9.0]

### 安全加固

- **严重安全漏洞修复**：
  - 修复权限绕过问题：aiocqhttp 平台必须同时在 authorized_user_ids 白名单中
  - 加固命令沙箱：移除 python/node/npm/pip/ssh/curl/wget/git 等危险命令
  - 新增危险字符过滤：换行符、引号、美元符号等
  - 修复 API 密钥泄露：api_get_config 不再返回明文 API key
  - 增强长期记忆路径安全：uid 参数校验，防止路径遍历攻击

## [2.8.9]

### 新增功能

- **回复最大字符数同步控制**：`tts_max_length` 配置项同步控制文字输出与语音播报，两者使用同一份截断后内容，不再出现文字超长而语音被截断的不一致情况
- **群聊人物区分**：群聊中每个用户的长期记忆独立存储（`group_{gid}:{sender_id}`），避免其他用户的聊天内容影响当前用户的回复，消除上下文感染

### 安全加固

- **电脑操作权限安全增强**：
  - 命令执行白名单：移除危险命令（python/node/npm/pip/ssh/curl/wget/git），只允许安全的系统命令
  - 危险字符过滤：新增换行符、引号、美元符号等过滤，防止代码注入
  - 权限逻辑修复：必须同时在 authorized_user_ids 白名单中才能在 aiocqhttp 平台使用
- **长期记忆路径安全**：增加 uid 参数校验，防止路径遍历攻击，确保文件路径在 memory 目录下
- **API 密钥保护**：`api_get_config` 不再返回 API key，敏感字段已脱敏

## [2.8.8]

### 新增功能

- **表情包识图识别**：表情包（含卡通图默认）先由识图模型识别图片内容，优先提取图中文字（无文字时退回约20字面部表情与情绪描述），再传给对话模型给予贴切回复
- **无/前缀指令触发**：直接发送指令名即可触发对应功能（如"聊天菜单"、"开启聊天"），无需添加 `/` 前缀
- **防拦截其他插件**：群聊中带 `/` 前缀的指令未 @ 机器人时，本插件不拦截，放行给其他插件处理
- **指令消息过滤**：本插件或其他插件的 `/` 指令消息不触发 AI 回复

### 修复问题

- **识图模型开关彻底生效**：关闭识图模型后，收到图片（含引用图片）不再触发任何回复
- **识图模型未配置时不走对话模型**：未配置识图模型时，图片消息直接跳过、不回复，避免误走对话模型产生无关回复
- **微信适配器兼容**：微信平台不支持 Record 语音组件，自动降级为纯文字发送，修复微信无法收到回复的问题
- **引用图片识别回归修复**：配置识图模型后，群聊引用图片（Reply）仍能正常识别回复

### 优化改进

- **@机器人唤醒回复**：不再提及"被@了"，像普通聊天一样自然回应
- **回复提示词精简**：简化 @机器人、表情包、图片场景的提示词，回复更自然简洁
- **表情包智能识别**：扩展 EMOJI_MEANINGS 字典，完整覆盖各类表情符号含义，自动识别 QQ/微信表情包
- **引用图片回复**：支持群聊中引用带表情包消息时自动识别并回复
- **GIF 第一帧提取**：自动将 GIF 表情包截取第一帧后再识图
- **识图提示词自定义**：可自定义识图提示词，灵活控制图片描述方式
- **表情包回复优化**：识图模型识别后优先提取图中文字，无文字时退回约20字面部表情与情绪描述，对话模型据此给予贴切回复

### 技术细节

- 插件优先级提升至 `inf`，确保优先于 AstrBot 默认对话插件
- 支持 GIF 图片预处理
- 语音模式逻辑分离更清晰
- 群聊无/前缀指令直接触发，无需 @ 机器人
- 群聊带 `/` 前缀指令未 @ 机器人时放行给其他插件
- 表情包识别流程：识图模型识别图片 → 判断类型 → 优先提取图中文字（无文字时生成约20字描述，重点面部表情与情绪）→ 传给对话模型（不传原图）
